# SQL Server to Snowflake Query Performance Comparison Tool
# Python 3.8+ required

# SQL Server connectivity
pyodbc>=4.0.39,<5.0.0

# Snowflake connectivity
snowflake-connector-python>=3.0.0,<4.0.0

# Data manipulation and comparison
pandas>=1.5.0,<3.0.0

# Configuration file parsing (YAML support)
pyyaml>=6.0,<7.0

# Configuration management
dynaconf>=3.2.0,<4.0.0

# Testing framework (for development)
pytest>=7.0.0,<8.0.0
pytest-cov>=4.0.0,<5.0.0

# Code quality tools (for development)
black>=23.0.0,<24.0.0
flake8>=6.0.0,<7.0.0

# Note: tkinter is included in Python standard library (no installation needed)
# Note: configparser is included in Python standard library (no installation needed)
# Note: concurrent.futures is included in Python standard library (no installation needed)

